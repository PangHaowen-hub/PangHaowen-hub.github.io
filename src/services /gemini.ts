// Gemini service removed as per request to remove AI features

export const initChatSession = () => {
  return null;
};

export const sendMessageStream = async (message: string) => {
    console.log(message);
    return null;
};
